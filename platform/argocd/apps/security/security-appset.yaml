apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: security-helm
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - list:
        elements:
          # Vault: secret store backend, file storage, standalone mode
          - name: vault
            chart: vault
            helmRepoURL: https://helm.releases.hashicorp.com
            chartVersion: 0.32.0
            namespace: vault
            valuesFile: platform/security/vault/values.yaml

          # External Secrets Operator: syncs Vault secrets → Kubernetes Secrets
          - name: external-secrets
            chart: external-secrets
            helmRepoURL: https://charts.external-secrets.io
            chartVersion: 2.1.0
            namespace: external-secrets
            valuesFile: platform/security/external-secrets/values.yaml

          # Keycloak: OIDC identity provider for ArgoCD, Grafana
          - name: keycloak
            chart: keycloakx
            helmRepoURL: https://codecentric.github.io/helm-charts
            chartVersion: 7.1.9
            namespace: keycloak
            valuesFile: platform/security/keycloak/values.yaml

  template:
    metadata:
      name: "{{ .name }}"
      namespace: argocd
      labels:
        app.kubernetes.io/part-of: security
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      sources:
        - repoURL: "{{ .helmRepoURL }}"
          chart: "{{ .chart }}"
          targetRevision: "{{ .chartVersion }}"
          helm:
            valueFiles:
              - "$values/{{ .valuesFile }}"
        - repoURL: https://github.com/mmrajput/kubernetes-platform-foundation
          targetRevision: HEAD
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .namespace }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
          - ServerSideApply=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: security-git
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - list:
        elements:
          # ESO ClusterSecretStore: vault-backend pointing to vault.vault.svc.cluster.local
          - name: external-secrets-config
            path: platform/security/external-secrets/config
            namespace: external-secrets

          # ExternalSecrets: operators/, databases/, workloads/ — all ESO CRs
          - name: external-secrets-stores
            path: platform/security/external-secrets/stores
            namespace: databases

  template:
    metadata:
      name: "{{ .name }}"
      namespace: argocd
      labels:
        app.kubernetes.io/part-of: security
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/mmrajput/kubernetes-platform-foundation
        targetRevision: HEAD
        path: "{{ .path }}"
        directory:
          recurse: true
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .namespace }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
          - ServerSideApply=true
      ignoreDifferences:
        - group: external-secrets.io
          kind: ExternalSecret
          jqPathExpressions:
            - .metadata.finalizers
            - .spec.target.deletionPolicy
            - .spec.target.template.engineVersion
            - .spec.target.template.mergePolicy
            - .spec.data[].remoteRef.conversionStrategy
            - .spec.data[].remoteRef.decodingStrategy
            - .spec.data[].remoteRef.metadataPolicy
